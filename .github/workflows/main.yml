name: deploy                                 

on:                                               
  push:
    branches:
      - main

jobs:                                                    
  deploy:
    runs-on: ubuntu-latest
    #environment: production

    steps:
      - name: Checkout                                      
        uses: actions/checkout@v3

      - name: Deploy
        uses: ./template                                                                                                                      
        with:
          version: v1.0.0
          namespace: "stage"

      # - name: Run Hello Script
      #   run: |
      #     #!/bin/bash
      #     echo "hello"


